# LEO CONNECT V2 Premium
Node/Express SaaS MVP. Render: Build `npm install`, Start `npm start`.
Environment: `JWT_SECRET`, `BASE_URL`, optional `DB_FILE`.
GitHub main branch is auto-deployed by Render. Test the site before connecting a custom domain.
